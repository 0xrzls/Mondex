import { appKit } from "../appkitConfig";
import { W3mConnectButton } from "@reown/appkit";

const WalletConnect = () => {
  return <W3mConnectButton appKit={appKit} />;
};

export default WalletConnect;
